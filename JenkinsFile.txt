pipeline {
tools{
	maven 'maven 3'
	}
	node('auto-ui-slave-1') {
	
    stage('Clone sources'){
		deleteDir()
		print "java version:"
		sh 'java -version'
		sh 'mvn --version'
		
        git branch: env.Branch, credentialsId: 'nikitha-kyndryl', poll: false, url: 'git@github.kyndryl.net:Rama-Bisht1/Selenium-Java-Poc.git'
    }
    }
	}